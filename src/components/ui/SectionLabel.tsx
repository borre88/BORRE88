// l'elemento che ricorre ovunque e dà il tono da referto
export const SectionLabel = ({ children }: { children: React.ReactNode }) => (
  <div className="font-ui font-bold text-[10.5px] uppercase tracking-[0.12em] text-gold">{children}</div>
);
